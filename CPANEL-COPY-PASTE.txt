═══════════════════════════════════════════════════════════
  RakuShopBD — cPanel এ copy-paste করুন (Mac Terminal নয়)
═══════════════════════════════════════════════════════════

⓪ গুরুত্বপূর্ণ — node_modules মুছুন (লাল error fix)
   File Manager → repositories/rakushopbd →
   "node_modules" ফোল্ডার DELETE করুন (পুরো folder)
   (cPanel নিজে symlink বানাবে — real folder থাকলে error)

① cPanel ব্রাউজার → Terminal খুলুন

② এই লাইনগুলো একটার পর একটা paste করুন:

cd ~/repositories/rakushopbd

git pull origin main

grep "registerAdminAuth" server.js
(লাইন দেখালে নতুন code আছে)

source /home/rakushop/nodevenv/repositories/rakushopbd/20/bin/activate

npm install

npm run admin:sync

③ cPanel → Setup Node.js App
   → Run NPM Install  (node_modules মুছার পর)
   → STOP APP → ১০ সেকেন্ড → START

touch tmp/restart.txt

④ ব্রাউজারে টেস্ট:
   https://rakushopbd.com/api/admin/version
   (দেখতে হবে: "apiVersion":2)

⑤ Admin login:
   https://rakushopbd.com/admin
   User: admin@rakushopbd.com
   Pass: BDRakuadmin2026%%

⑥ public_html/index.html মুছে দিন (থাকলে)

───────────────────────────────────────────────────────────
যদি cd ~/repositories/rakushopbd fail হয়:
  File Manager → যেখানে server.js → সেই path এ cd করুন

যদে npm not found:
  Node.js App → Stop → Run NPM Install → Start
  তারপর virtualenv line আবার চালান

যদি git pull না হয়:
  Git Version Control → Pull বাটন
  অথবা GitHub ZIP → File Manager upload
───────────────────────────────────────────────────────────
